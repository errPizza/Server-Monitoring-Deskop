# Optional browser preview configuration (npm run web).
EXPO_PUBLIC_APP_ENV=mock
EXPO_PUBLIC_API_BASE_URL=https://www.anothergamemore.online/api
# Electron reads AGM_MODE and AGM_API_URL from the process environment.
# See README.md for PowerShell and Linux examples.
